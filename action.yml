name: 'Say hello'
description: 'Greet someone'
inputs:
  name:
    description: "Someone's name"
    required: true
    default: 'Somebody'
outputs:
  reply:
    value: "aaa"

runs:
  using: 'node20'
  main: './main.mjs'